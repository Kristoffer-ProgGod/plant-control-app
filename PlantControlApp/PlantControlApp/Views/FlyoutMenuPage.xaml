<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PlantControlApp.Views"
             xmlns:models="clr-namespace:PlantControlApp.Models"
             x:Class="PlantControlApp.Views.FlyoutMenuPage"
             Padding="0,40,0,0"
             IconImageSource="hamburger.png"
             Title="Personal Organiser">
    <StackLayout>
        <ListView x:Name="listView" x:FieldModifier="public" BackgroundColor="White">
            <ListView.ItemsSource>
                <x:Array Type="{x:Type models:FlyoutPageItem}">
                    <models:FlyoutPageItem Title="LookAtThisGraph" IconSource="GraphIcon.png" TargetType="{x:Type local:DataView}" />
                    <models:FlyoutPageItem Title="CreatePairing" IconSource="Pairing.png" TargetType="{x:Type local:PairingView}"/>
                    <models:FlyoutPageItem Title="Plants" IconSource="PlantListIcon.png" TargetType="{x:Type local:PlantView}"/>
                </x:Array>
            </ListView.ItemsSource>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="5,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image Source="{Binding IconSource}" Aspect="AspectFit"/>
                            <Label Grid.Column="1" Text="{Binding Title}" TextColor="Black" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>